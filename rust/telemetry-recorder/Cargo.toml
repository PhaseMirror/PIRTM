[package]
name = "telemetry-recorder"
version = "1.0.0-alpha"
edition = "2021"
description = "Local telemetry recording for pirtm-compiler events"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
