[package]
name = "adr-verifier"
version = "0.1.0"
edition = "2021"
authors = ["Phase Mirror Contributors"]
license = "MIT OR Apache-2.0"
description = "Deterministic zero-dependency ADR registry verifier and kernel guard"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[[bin]]
name = "adr-verifier"
path = "src/main.rs"
